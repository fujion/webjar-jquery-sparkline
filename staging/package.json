{
  "name": "${project.artifactId}",
  "version": "${version.unrevise}",
  "description": "${project.description}",
  "scripts": {
    "postinstall": "gulp"
  },
  "dependencies": {
    "jquery-sparkline": "${version.unrevise}"
    },
  "devDependencies": {
    "gulp": "^4.0.0"
  }
}
